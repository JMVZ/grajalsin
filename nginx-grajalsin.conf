server {
    listen 80;
    listen [::]:80;
    server_name grajalsin.com.mx www.grajalsin.com.mx;
    
    # Redirigir HTTP a HTTPS (descomentar cuando tengas SSL)
    # return 301 https://$server_name$request_uri;

    root /var/www/grajalsin/Grajalsin/public;
    index index.php index.html index.htm;

    charset utf-8;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }

    error_page 404 /index.php;

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.3-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_hide_header X-Powered-By;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }

    # Optimización de assets estáticos
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }
}

# Configuración HTTPS (descomentar y configurar cuando tengas certificado SSL)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name grajalsin.com.mx www.grajalsin.com.mx;
# 
#     ssl_certificate /etc/letsencrypt/live/grajalsin.com.mx/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/grajalsin.com.mx/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
# 
#     root /var/www/grajalsin/Grajalsin/public;
#     index index.php index.html index.htm;
# 
#     charset utf-8;
# 
#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }
# 
#     location = /favicon.ico { access_log off; log_not_found off; }
#     location = /robots.txt  { access_log off; log_not_found off; }
# 
#     error_page 404 /index.php;
# 
#     location ~ \.php$ {
#         fastcgi_pass unix:/var/run/php/php8.3-fpm.sock;
#         fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
#         include fastcgi_params;
#         fastcgi_hide_header X-Powered-By;
#     }
# 
#     location ~ /\.(?!well-known).* {
#         deny all;
#     }
# 
#     location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#         access_log off;
#     }
# }

